@model IEnumerable<DoAnLTWeb2.Models.Booking>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}



<main id="main" class="main">
    <div class="pagetitle">
        <h2>Danh sách đặt bàn</h2>
    </div>

    <section class="section dashboard">
        <div class="row">
            <div class="col-12">
                <div class="card recent-sales overflow-auto">
                    <div class="card-body mt-4">
                        <table class="table table-borderless datatable">
                            <thead>
                                <tr>
                                    <th class="col-1 text-center">Thứ tự</th>
                                    <th class="col-2 text-center">Tên khách hàng</th>
                                    <th class="col-2 text-center">Số điện thoại</th>
                                    <th class="col-3 text-center">Ngày đặt</th>
                                    <th class="col-2 text-center">Số người</th>
                                    <th class="col-3 text-center">Chức năng</th>

                                </tr>
                            </thead>
                            <tbody>
                                @{
                                    int i = 1;
                                }
                                @foreach (var item in Model)
                                {
                                    <tr id="tr_@item.OrderID">
                                        <td class="text-center">@(i++)</td>
                                        <td class="text-center">@item.CusName</td>
                                        <td class="text-center">@item.Phone</td>
                                        <td class="text-center">@item.OrderDate</td>                                      
                                        <td class="text-center">@item.NumberOfPeople</td>                                      
                                        <td class="text-center">
                                            <a class="btn btn-primary btn-sm" href="@Url.Action("Details", "Bookings", new { id = item.OrderID })">Chi tiết</a> |
                                            <a class="btn btn-danger btn-delete btn-sm" href="@Url.Action("Delete", "Bookings", new { id = item.OrderID })">Xóa</a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </section>
</main>